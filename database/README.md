# Garage Vision Database Schema

This folder contains the live exported MySQL schema for Garage Vision.

To load the schema into a local database:

```bash
mysql -u root -p garage_db < garage_schema.sql
